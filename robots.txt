# Dan's Island — un jeu de détente. https://dansisland.app
#
# Tout est ouvert : les îles sont publiées par leurs propriétaires, et une
# île qu'on ne peut pas trouver ne reçoit pas de visite. Ce qui n'est pas
# publié n'est pas servi — c'est la RLS qui s'en charge, pas ce fichier.

User-agent: *
Allow: /

# Pas de ligne `Sitemap:` ici, et c'est délibéré depuis le 18/09/2026 au
# soir : le plan du site était fabriqué à la demande par une Cloudflare
# Pages Function, et ces fonctions sont sorties du dépôt (voir README).
# Annoncer un plan à une adresse qui rend la page du jeu — le catch-all de
# `_redirects` répond 200 à tout — vaut moins que ne rien annoncer du tout.
# La ligne revient avec les fonctions.
